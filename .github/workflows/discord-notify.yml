name: Notificar a Discord con curl

on:
  push:
    branches:
      - main  # o 'master', según tu rama principal

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar notificación a Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{\"content\": \"📢 Push detectado en *${{ github.repository }}* por **${{ github.actor }}** en la rama \`${{ github.ref_name }}\`\"}" \
          $DISCORD_WEBHOOK
